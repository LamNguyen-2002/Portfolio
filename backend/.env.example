# Backend environment — copy to .env and fill in (local dev)
# Port the Express API listens on (Render sets PORT automatically).
PORT=5001

# Secret used to sign JWT tokens (HMAC SHA256). Use a long random string.
# On Render this is generated automatically (see render.yaml).
JWT_SECRET=change_me_to_a_long_random_secret
