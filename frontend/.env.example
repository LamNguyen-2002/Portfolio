# Frontend environment — copy to .env (or .env.local)
#
# Để TRỐNG khi chạy local: API gọi tới /api/* và được Vite proxy sang backend
# (xem vite.config.js). Khi deploy, đặt bằng URL backend đã deploy, ví dụ:
#   VITE_API_BASE=https://portfolio-backend.onrender.com
VITE_API_BASE=
